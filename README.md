# Assignment
The students are stored in a simple in-memory list, and the program allows you to:  Add a new student (name, age, grade)  Display students in a Tkinter table (Treeview)  Refresh the table when new data is added  Show status messages (like missing fields or successful additions)
